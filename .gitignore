# Ignore Hugo generated public directory (only needed for deployment)
public/

# Hugo build output - generated files (not needed in version control)
resources/_gen/

# Hugo cache directory (stores temporary files)
.hugo_cache/

# Optional: ignore the Hugo binary if stored in the repo
hugo

# Ignore node_modules if using JS dependencies for Hugo
node_modules/

# macOS system files (generated automatically, should be ignored)
.DS_Store

# Log files from macOS and Hugo
*.log

# Backup files created by some macOS apps
*~ 
*.swp 

# IDE/editor files (VS Code, Vim, etc.)
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Ignore temporary files created during development
thumbs.db
npm-debug.log
yarn-debug.log
yarn-error.log
